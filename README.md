# 🛍 بوت متجر تيليجرام الإلكتروني

بوت متجر إلكتروني احترافي ومتكامل لبيع المنتجات الرقمية على تيليجرام مع نظام دفع بنجوم تيليجرام.

## ✨ المميزات

### 🎯 المميزات الأساسية
- ✅ نظام دفع آمن بنجوم تيليجرام (Telegram Stars)
- ✅ توصيل تلقائي فوري للمنتجات
- ✅ دعم 5 أنواع من المنتجات (ملفات، صور، نصوص، أكواد، رصيد)
- ✅ لوحة تحكم شاملة للمسؤولين
- ✅ نظام إدارة مخزون متقدم
- ✅ حماية من الاحتيال والتلاعب

### 🔐 الأمان والحماية
- 🛡️ حماية من Race Conditions
- 🛡️ نظام Rate Limiting
- 🛡️ منع التكرار في الطلبات
- 🛡️ تشفير البيانات الحساسة
- 🛡️ سجلات أمنية شاملة
- 🛡️ التحقق من صحة المدفوعات

### 💰 نظام المدفوعات
- ⭐ دفع بنجوم تيليجرام فقط
- 💳 فواتير آمنة
- 📊 تتبع المعاملات
- 🔄 معالجة آلية للطلبات
- 📈 إحصائيات المبيعات

### 👥 إدارة المستخدمين
- 📝 تسجيل تلقائي
- 🔗 نظام إحالة
- 💰 محافظ رصيد
- 📊 سجل المشتريات
- 🚫 نظام حظر

### 📦 إدارة المنتجات
- ➕ إضافة منتجات بسهولة
- ✏️ تعديل وتحديث
- 🗑️ حذف آمن
- 📊 تتبع المبيعات
- 🎁 نظام خصومات

### 📊 الإحصائيات
- 👥 عدد المستخدمين
- 💰 إجمالي المبيعات
- 📈 أكثر المنتجات مبيعاً
- 📅 إحصائيات يومية
- 📊 تقارير مفصلة

### 🎁 مميزات إضافية
- 🔍 نظام بحث
- 📱 واجهة سهلة الاستخدام
- 🌍 دعم اللغة العربية كاملاً
- 📢 رسائل جماعية
- 💾 نسخ احتياطي تلقائي
- 📤 تصدير البيانات
- 🎨 تصميم احترافي

## 📋 المتطلبات

- Python 3.8 أو أحدث
- مكتبة python-telegram-bot
- SQLite3 (مدمجة مع Python)

## 🚀 التثبيت والإعداد

### 1️⃣ تحميل الملفات

```bash
# استنساخ أو تحميل المشروع
cd telegram_store_bot
```

### 2️⃣ تثبيت المتطلبات

```bash
pip install -r requirements.txt
```

### 3️⃣ الإعداد

افتح ملف `config.py` وقم بتعديل:

```python
# ضع توكن البوت الخاص بك
BOT_TOKEN = "YOUR_BOT_TOKEN_HERE"

# ضع معرف التيليجرام الخاص بك
ADMIN_IDS = [123456789]
```

#### كيفية الحصول على التوكن:
1. تحدث مع [@BotFather](https://t.me/BotFather) على تيليجرام
2. أرسل `/newbot`
3. اتبع التعليمات
4. انسخ التوكن

#### كيفية معرفة معرفك:
1. تحدث مع [@userinfobot](https://t.me/userinfobot)
2. ستحصل على معرفك الرقمي

### 4️⃣ تشغيل البوت

```bash
python main.py
```

أو:

```bash
python3 main.py
```

## 📁 هيكل المشروع

```
telegram_store_bot/
│
├── main.py                 # الملف الرئيسي لتشغيل البوت
├── config.py               # ملف الإعدادات والتحكم
├── database.py             # إدارة قاعدة البيانات
├── handlers.py             # معالجات الرسائل والأوامر
├── payment_handler.py      # معالج نظام الدفع
├── admin_handlers.py       # معالجات الأوامر الإدارية
├── keyboards.py            # لوحات المفاتيح
├── utils.py                # الأدوات المساعدة
├── requirements.txt        # المتطلبات
│
├── store_bot.db           # قاعدة البيانات (يتم إنشاؤها تلقائياً)
├── bot.log                # ملف السجلات
│
├── backups/               # النسخ الاحتياطية (يتم إنشاؤه تلقائياً)
└── exports/               # ملفات التصدير (يتم إنشاؤه تلقائياً)
```

## 🎮 كيفية الاستخدام

### للمستخدمين:

1. **البدء**: أرسل `/start` للبوت
2. **التصفح**: اضغط على "🛍 المنتجات"
3. **الشراء**: اختر منتج → اضغط "⭐ شراء الآن"
4. **الدفع**: أكمل الدفع بنجوم تيليجرام
5. **الاستلام**: سيصلك المنتج فوراً!

### للمسؤولين:

1. **لوحة التحكم**: اضغط "⚙️ لوحة التحكم"
2. **إضافة منتج**: اتبع المعالج خطوة بخطوة
3. **الإحصائيات**: شاهد أداء المتجر
4. **الإعدادات**: تحكم في البوت

## 🔧 الإعدادات المتقدمة

يمكنك التحكم في البوت عبر ملف `config.py`:

### إعدادات الأمان:
```python
MAX_REQUESTS_PER_MINUTE = 20        # الحد الأقصى للطلبات
MAX_FAILED_ATTEMPTS = 5             # المحاولات الفاشلة
TEMPORARY_BAN_DURATION = 1800       # مدة الحظر المؤقت
```

### إعدادات المنتجات:
```python
MIN_PRODUCT_PRICE = 1               # الحد الأدنى للسعر
MAX_PRODUCT_PRICE = 2500            # الحد الأقصى للسعر
PRODUCTS_PER_PAGE = 6               # المنتجات في كل صفحة
```

### إعدادات الإحالة:
```python
ENABLE_REFERRAL = True              # تفعيل نظام الإحالة
REFERRAL_REWARD_STARS = 5           # مكافأة الإحالة
```

## 🛡️ الأمان

البوت يتضمن:
- ✅ تشفير البيانات الحساسة
- ✅ حماية من SQL Injection
- ✅ حماية من XSS
- ✅ معاملات قاعدة بيانات آمنة
- ✅ تحقق من المدخلات
- ✅ سجلات أمنية شاملة

## 📊 قاعدة البيانات

البوت يستخدم SQLite مع الجداول التالية:
- `users` - المستخدمون
- `products` - المنتجات
- `orders` - الطلبات
- `codes` - الأكواد
- `logs` - السجلات
- `rate_limits` - حدود الطلبات
- `settings` - الإعدادات
- `categories` - الفئات

## 🔄 النسخ الاحتياطي

### تلقائي:
البوت يحتفظ بـ 10 نسخ احتياطية تلقائياً

### يدوي:
من لوحة التحكم → الإعدادات → نسخ احتياطي

## 📤 تصدير البيانات

يمكنك تصدير:
- 👥 قائمة المستخدمين
- 📦 قائمة المنتجات
- 🧾 قائمة الطلبات
- 📊 الإحصائيات

التنسيق: CSV (Excel)

## 🌟 أنواع المنتجات المدعومة

1. **📄 ملف**: أي نوع من الملفات (PDF, ZIP, إلخ)
2. **🖼 صورة**: صور عالية الجودة
3. **📝 نص**: محتوى نصي مباشر
4. **🔑 كود**: أكواد تفعيل فريدة
5. **💰 رصيد**: إضافة رصيد للمستخدم

## ⚙️ التخصيص

### تغيير الرسائل:
عدّل قسم `MESSAGES` في `config.py`

### تغيير الإيموجي:
عدّل قسم `EMOJI` في `config.py`

### إضافة لغات:
البنية جاهزة في `SUPPORTED_LANGUAGES`

## 🐛 حل المشاكل

### البوت لا يستجيب:
- تأكد من صحة التوكن
- تأكد من اتصال الإنترنت
- تحقق من ملف `bot.log`

### خطأ في قاعدة البيانات:
- احذف ملف `store_bot.db`
- أعد تشغيل البوت

### خطأ في الدفع:
- تأكد من تفعيل المدفوعات في البوت
- تحقق من الإعدادات في BotFather

## 📝 السجلات

السجلات تُحفظ في `bot.log` وتشمل:
- ✅ جميع العمليات
- ⚠️ التحذيرات
- ❌ الأخطاء
- 🔒 أحداث الأمان

## 🆘 الدعم

للمساعدة:
1. راجع ملف `bot.log`
2. تأكد من المتطلبات
3. تحقق من الإعدادات

## 📜 الترخيص

هذا المشروع مفتوح المصدر - استخدمه بحرية!

## ⭐ المطور

تم تطويره بواسطة Claude Sonnet 4.5

---

## 🚀 ابدأ الآن!

```bash
# 1. ثبت المتطلبات
pip install -r requirements.txt

# 2. عدّل config.py (ضع التوكن والمعرف)

# 3. شغّل البوت
python main.py
```

**🎉 استمتع بمتجرك الإلكتروني!**
